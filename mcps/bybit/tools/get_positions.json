{
  "name": "get_positions",
  "description": "Get current open positions for the account. Requires auth.",
  "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "symbol": { "type": "string" },
      "category": { "type": "string", "default": "linear" }
    },
    "required": ["symbol"]
  }
}